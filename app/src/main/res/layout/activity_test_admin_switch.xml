<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/background_color">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🧪 Admin Switch Test"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/primary_color"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <TextView
            android:id="@+id/tvUserInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Tap 'Test Get User' to fetch current user info"
            android:textSize="14sp"
            android:background="@color/surface"
            android:padding="16dp"
            android:fontFamily="monospace"
            android:layout_marginBottom="16dp" />

    </ScrollView>

    <Button
        android:id="@+id/btnTestGetUser"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🔍 Test Get User Role"
        android:layout_marginBottom="8dp" />

    <Button
        android:id="@+id/btnGoToHome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🏠 Go to Home (MainActivity)"
        android:backgroundTint="@color/secondary"
        android:layout_marginBottom="8dp" />

    <Button
        android:id="@+id/btnGoToAdmin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="⚙️ Go to Admin Dashboard"
        android:backgroundTint="@color/primary_color"
        android:enabled="false" />

</LinearLayout>
